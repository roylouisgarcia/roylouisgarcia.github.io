# Top-level paths that make up the live site. Only what's listed here
# gets uploaded by deploy.sh -- anything not listed (dev scripts, process
# notes, README, .git, etc.) never leaves this machine, regardless of
# what else ends up sitting in the repo root.
#
# Directories are listed without a trailing slash, so rsync copies the
# directory itself (e.g. "css" -> public_html/css/), not just its contents.
index.html
protege.pdf
css
js
images
media
sound
specialization
portfolioentries
certs_projects
