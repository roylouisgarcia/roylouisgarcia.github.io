# RhythmBrownBox — Version 3

**Planned. Not started.**

Candidate directions, carried over from v2's "still open" list:

- Roving-tabindex ARIA grid — one tab stop for the whole grid, arrow keys to
  move within it (v2 makes every cell its own tab stop).
- Swing / shuffle.
- Per-track pattern lengths (polymeter — different-length rows playing against
  each other).
- Possible reach: song mode (chained patterns), a fuller kit, simple effects.

This folder and its `documentation/` are placeholders until work begins.
